{
  "globalEnv": ["NODE_ENV"],
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "lint": {},
    "build": {
      "outputs": ["dist/**", ".next/**", "*.artifact.js"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": ["coverage/lcov-info"]
    }
  }
}
