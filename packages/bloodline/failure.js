// since madge was designed to have warnings as a stateful value,
// we are likely going to use the rejection case of Futures
// (but this remains to be seen as a viable solution, so this is just a stub for now)
export const handleWarnings = failureState => {}
