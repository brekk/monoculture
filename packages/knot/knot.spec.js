import {
  prepend,
  append,
  cake,
  markdownTabs,
  nthIndexOf,
  nthLastIndexOf,
  nthIndex,
} from './knot'

test('prepend', () => {
  expect(prepend).toBeTruthy()
})
test('append', () => {
  expect(append).toBeTruthy()
})
test('cake', () => {
  expect(cake).toBeTruthy()
})
test('markdownTabs', () => {
  expect(markdownTabs).toBeTruthy()
})
test('nthIndexOf', () => {
  expect(nthIndexOf).toBeTruthy()
})
test('nthLastIndexOf', () => {
  expect(nthLastIndexOf).toBeTruthy()
})
test('nthIndex', () => {
  expect(nthIndex).toBeTruthy()
})
